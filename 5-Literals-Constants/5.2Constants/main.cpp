#include <iostream>

// 'const' keyword is used to define a keyword, whose value cannot be updated once after it's initialization

int main() {
    const int weightLimit = 150;
    const int minHeight = 170;

    const float PI = 3.14159;

    const std::string galaxy = "Milky way";
    // const keyword can be applied to local, global and memeber variables

    return 0;
}
