#include <iostream>

#include "BST.h"
#include "Treenode.h"
#include "priority_queue.h"
using namespace std;

int main() {
    /**  Queue queue;
     queue.enqueue(69, 0);
     queue.enqueue(100, 3);
     queue.enqueue(420, 1);
     queue.enqueue(101, 3);
     queue.enqueue(102, 2);

     queue.dequeue();
     queue.dequeue();
     queue.dequeue();
     queue.dequeue();
     queue.print();
 **/
    /**
        BST *tree = new BST(new Treenode(10));
        tree->insert(6);
        tree->insert(2);
        tree->insert(8);
        tree->insert(20);
        tree->insert(15);
        tree->insert(25);

        cout << "===== printing ======" << endl;
        tree->printSideways();

        int find = 10;
        if (tree->contains(find))
            cout << "Found" << endl;
        else
            cout << "NOT Found" << endl;

        tree->remove(find);

        cout << "===== printing ======" << endl;
        tree->printSideways();

        delete tree;
    */
}
